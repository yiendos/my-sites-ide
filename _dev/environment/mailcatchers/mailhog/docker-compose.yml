services:
    mailhog:
        container_name: "${NAMESPACE}_mailhog"
        image: ${NAMESPACE}_mailhog:latest
        build:
            context: .
            dockerfile: Dockerfile
        env_file: 
            - ./.env
            - ./../../../../.env
        networks:
            - my-sites-ide