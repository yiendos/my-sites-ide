services:
    node:
        container_name: "${NAMESPACE}_node"
        image: ${NAMESPACE}_frontend:latest
        env_file: 
            - ./.env
            - ./../../../../.env
        volumes:
            - './../../../../Sites:/opt/sites'
            - '../../../../Packages:/opt/Packages'
#        networks:
#            - my-sites-ide