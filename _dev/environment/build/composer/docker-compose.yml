services:
    composer:
        container_name: "${NAMESPACE}_composer"
        image: ${NAMESPACE}_composer:latest
        build:
            context: .
            dockerfile: Dockerfile
        env_file: 
            - ./.env
            - ./../../../../.env
        volumes:
            - './../../../../Repos:/opt/repos'
            - '../../../../Packages:/opt/Packages'
#        networks:
#            - my-sites-ide